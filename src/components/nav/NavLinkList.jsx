import styled from 'styled-components';

const NavLinkList = styled.ul`
    padding: 0;
    list-style: none;
    display: flex;
    align-items: center;
    width: 100%;
`

export default NavLinkList;
